# Shared Templates

General-purpose templates reused across workflows.

- `roles/` - reusable role definitions used by workflow steps
- `skills/` - reusable skill modules referenced by roles and steps

Also:
- `log_decisions.md` - optional prompt to reconcile/lock decisions
